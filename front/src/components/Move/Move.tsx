import React from 'react';
import {dbRecord} from "types";

interface Props {
  currPositionID: string,
  record: dbRecord,
}

const getDiffIndex = (id1: string, id2: string): string => {

}

export const Move = (props: Props) => {
  
  
  return (
    <>
    
    </>
  );
};